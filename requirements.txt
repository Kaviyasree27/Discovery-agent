# Core
flask>=3.0.0
groq>=0.9.0
werkzeug>=3.0.0

# PDF extraction (Level 1 — PDF support)
PyMuPDF>=1.24.0

# Excel/spreadsheet extraction (Level 1 — XLSX support)
openpyxl>=3.1.0
xlrd>=2.0.1

# Image OCR fallback (optional — vision model used first)
# Install if Groq vision is unavailable:
# pip install pytesseract Pillow
# Also requires: sudo apt install tesseract-ocr
